# ⚠️ Reference only — do NOT run

One-off fix scripts whose logic has been folded into the numbered migrations.
They do not reflect the current schema. If you need something from a file in
here, port it into a new numbered migration (see
[`../MIGRATIONS.md`](../MIGRATIONS.md)) — never run the file directly.
