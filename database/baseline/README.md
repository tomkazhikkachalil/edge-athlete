# database/baseline

`000_rebuild.sql` — the live schema as one guarded, dependency-ordered SQL
file; what a blank Supabase project runs to become this one. GENERATED by
`npm run build:baseline` over `public.schema_dump()` (migration 227); never
edited by hand. `database/MIGRATIONS.md` → "To build an environment" is the
runbook. First generated Sep 21 2026 from prod at ledger head 227 (122
tables, 107 functions, 172 public + 7 storage policies, 106 triggers, 4
buckets, 100 reserved handles). Regenerate after any migration; the header
names the head it embodies. pg_cron jobs are NOT in it (cron.job is not
readable by the service role) — recreate the two from 059 and 135 by hand.
