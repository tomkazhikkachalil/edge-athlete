{
  "$comment": "Documented exceptions to the provenance rule (every live table and column is named by a numbered migration). Each entry cites where the object came from and which migration will record it. `npm run check:schema` fails on drift NOT listed here and on entries that have become stale — this file only ever shrinks. EMPTY since the baselines 190–193 (data foundation P2–P5, Sep 14 2026): every live table and column is owned by the chain. A new entry needs a reason and the migration that will retire it.",
  "entries": []
}
